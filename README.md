# Safe Driving Analysis System

A Python-based tool to analyze risky driving behaviors, generate safety reports, and calculate fuel and carbon emission savings.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the dashboard:
   ```bash
   streamlit run src/app.py
   ```
